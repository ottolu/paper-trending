# Copy to .env (gitignored) and fill in. Never commit real keys.

# LLM — DeepSeek official, V4 Pro (https://platform.deepseek.com).
DEEPSEEK_API_KEY=

# Embedding — SiliconFlow, Qwen3-Embedding-8B (https://siliconflow.cn).
# NOTE: the old SiliconFlow key leaked publicly and was revoked — generate a NEW one.
SILICONFLOW_API_KEY=
